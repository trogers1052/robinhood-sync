# Robinhood Sync Service Dependencies (pinned to production versions)

# Shared platform infrastructure (metrics shim, Redis base, Telegram, daemon, config)
trading-py-commons @ git+https://github.com/trogers1052/trading-py-commons.git@v0.2.0

# Robinhood API
robin-stocks==3.4.0

# Kafka
kafka-python==2.3.0

# Redis (for tracking synced orders)
redis==7.0.1

# Configuration
python-dotenv==1.2.2
pydantic==2.13.2
pydantic-settings==2.13.1

# Date handling
python-dateutil==2.9.0.post0

# 2FA Support
pyotp==2.9.0

# Metrics
prometheus-client==0.24.1

# Testing
pytest==9.0.2
pytest-mock==3.15.1

# Event contract schemas (test-only: validates emitted Kafka payloads against
# the platform's single source of truth for Kafka event contracts)
trading-event-schemas @ git+https://github.com/trogers1052/trading-event-schemas@v0.1.1
